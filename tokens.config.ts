import { defineTokens, palette } from '@nuxtjs/design-tokens'

export default defineTokens({
  colors: {
    white: {
      value: '#FFFFFF'
    },
    primary: palette('#FFF'),
    secondary: palette('#FFF')
  }
})
